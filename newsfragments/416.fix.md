scripts: Only write the admin login and password when they actually change. Odoo sends a
security notification whenever those fields are written, not only when their value
differs, so every container start emailed every admin that their password had changed.
