This module removes the admin user (base.user_admin) from the Sales CRM team(sales_team.team_sales_department). As the
restricted users don't have access to the admin user they would get an Access Error when trying to access the Sales CRM
team if the admin user is part of the team
